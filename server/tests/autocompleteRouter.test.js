/* const { getArray } = require("../routers/autocompleteRouter");

describe("get autocomplete array", () => {
  test("prefix Z", () => {
    const input = "z";
    const result = getArray(input);
    expect(result).toEqual(["Zapdos", "Zubat"]);
  });
}); */
